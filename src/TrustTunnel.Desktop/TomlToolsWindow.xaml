<Window x:Class="TrustTunnel.Desktop.TomlToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать TOML" Width="720" Height="560" MinWidth="540" MinHeight="420"
        WindowStartupLocation="CenterOwner" UseLayoutRounding="True">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Редактировать TOML" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="Можно поправить redacted TOML и скопировать результат. Полный экспорт требует подтверждения." Foreground="{DynamicResource MutedTextBrush}" />
        </StackPanel>
        <TextBox x:Name="TomlTextBox" Grid.Row="1" FontFamily="Consolas" IsReadOnly="False"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Скопировать TOML" Click="CopyRedactedButton_Click" Style="{StaticResource GhostButton}" />
            <Button Content="Скопировать полный" Click="CopyFullButton_Click" />
            <Button Content="Закрыть" IsCancel="True" Style="{StaticResource GhostButton}" />
        </WrapPanel>
    </Grid>
</Window>
